# Python 3.12 optimized ML stack (Windows)

# Core dependencies
numpy>=2.0.0,<2.3.0
ujson>=5.10.0

# Extra features
emoji>=2.14.0
nltk>=3.9.0

# Protocols / API
fastapi>=0.116.0
pydantic>=2.11.0
uvicorn>=0.35.0
httpx>=0.28.0

# Language processing
# Keep transformers moderate to ensure Py3.12 wheels availability
transformers>=4.43.2,<4.56.0
# Use CPU or DirectML builds explicitly per environment
# For CPU-only (default):
# For DirectML/CPU on Windows (pins compatible with torch-directml)
torch==2.4.1
torchvision==0.19.1
torch-directml
# torchaudio has fewer prebuilt wheels on Windows; pin compatible if needed
# torchaudio==2.5.1
huggingface_hub>=0.23.0,<0.36.0
openai-harmony>=0.0.4

# Storage & JIT
numba>=0.60.0,<0.61.0
llvmlite>=0.43.0,<0.44.0

# Development
black>=23.7.0
flake8>=6.1.0
isort>=5.12.0
mypy>=1.5.0
psutil>=5.9.0
pytest>=7.4.0
questionary>=2.0.1
tqdm>=4.66.0
autopep8>=1.7.0

# Type stubs
types-tqdm>=4.66.0
types-ujson>=5.10.0
typing-extensions>=4.7.0
types-psutil

# Additional dependencies
gradio>=3.50.0
rich
gpt-oss>=0.0.4
tiktoken>=0.11.0
lxml>=6.0.0
regex>=2025.7.0
requests>=2.32.0

# Development tools
pyright>=1.1.0

zstandard>=0.22.0
