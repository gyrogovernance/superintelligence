> **CGM Science Documentation - Port** : This doc is copied from our science repo - all of its surrounding theory and relative paths can be found at https://github.com/gyrogovernance/science

# Analysis: Moment Receipts, QR Transport, and the FNV Profile

**Citation:** Korompilias, B. (2025). Common Governance Model: Mathematical Physics Framework. Zenodo. https://doi.org/10.5281/zenodo.17521384

## Overview

This document records the measurements of `experiments/hqvm_moments_fiat_analysis_1.py` and `experiments/hqvm_moments_fiat_analysis_2.py` and the structural convergence they exhibit. The subject is the Moments medium: the economy layer (MU, UHI, CSM capacity, tiers), the genealogy layer (depth-4 frame records, parity commitments), the NTP time coordinate, QR code transport of receipts, and the FNV-1a hash as the name layer. QR transport and the FNV profile are not part of the kernel specification; they are examined here as candidate infrastructure and measured against the kernel. Ubiquitous inference hosts are examined on the same footing, with the additional property that the kernel already occupies the model execution path, so a receipt issued from such a host reads coordinates the inference workload has already produced. The kernel formalism is defined in `docs/Gyroscopic_Computational_Theory/hQVM_Specs_Formalism.md` and `docs/Gyroscopic_Computational_Theory/hQVM_QuBEC_Theory.md`, the economic framework in `docs/Gyroscopic_Computational_Theory/AIR_Moments_Economy_Whitepaper.md`, the verification chain in `docs/Gyroscopic_Computational_Theory/Moments_Tests_Report.md`, and the Z2 holonomy in `docs/Findings/Analysis_hQVM_Wavefunction.md`. The central finding is that the receipt is not a data container with checks attached: it is a coordinate on a deterministic kernel trajectory, fully specified by an anchor, a depth, and a phase, with every proof field regenerable by replay, and the accounting units and the state geometry are the same calendar on a substrate inherited from physics and commodity infrastructure rather than constructed.

## 1. The Receipt as a Multi-Chart Object

A moment receipt is one object read under several native charts of the kernel at once. Time, carrier, code, chirality and gauge are not annotations attached after the fact; each field of the receipt is already a member of one of those spaces. The layout places exactly one field on each chart that the transport form needs:

| Field | Bytes | Chart | Native space | Content |
|-------|-------|-------|--------------|---------|
| sec32 | 4 | time | NTP 32.32 fixed point | which second since 1900 |
| frac32 | 4 | time | top 12 bits = omega12 address | which manifold state inside the second |
| identity24 | 3 | carrier | Omega, 4096 states | routed state of the identity bytes |
| seal24 | 3 | carrier | Omega, 4096 states | routed state of header and payload |
| parity24 | 3 | code | C64 x C64, self-dual [12,6,2] code | two mask codewords (O, E) of the payload |
| event byte | 1 | chirality + gauge | GF(2)^6 and K4 | transport class Q(payload), family label |

Every field is a member of a space the kernel already defines. No field is a declared enumeration. The event byte is derived from the payload's accumulated transport charge (measured: event byte 0x0E, intron 0xA4, family 2, micro 18, q6 0x2D, with q6(event byte) = Q(payload)), so what other designs call the event type is here a computed position on the chirality chart, checkable by any verifier. The width identities that recur throughout this analysis are the signature of this multi-chart placement: m12 is 12 bits because |Omega| = 2^12; the NTP timestamp is 64 bits in an architecture whose horizon, chirality register, mask code, and Walsh transform are all of size 64; the time field is 8 bytes, the exact width of the F-squared Z2 holonomy cycle; the Version 2-L QR frame holds 32 data codewords, the width of the kernel register atom (state24 plus intron, the SU(2) spinor atom of the specification).

## 2. The Inherited Grid

The NTP grid is a globally synchronized coordinate system that has been accumulating since 1900-01-01 UTC. Measured at the time of this analysis: 126.59 years elapsed, 1.715826 x 10^19 tick-coordinates at full frac32 resolution. These coordinates are ordered, unambiguous within an era, and agreed by every networked clock on Earth. The top 12 bits of each fraction partition every second into 4096 buckets of 244.140625 microseconds, one bucket per state of Omega, traversing the manifold forward once per second. A QR receipt therefore constructs nothing at the time layer: it claims one coordinate of a pre-existing universal track and binds content to it. The ledger's ordering substrate is inherited from the atomic time standard, which is the same physical source from which the CSM capacity envelope is derived (f_Cs = 9,192,631,770 Hz). Time addressing and capacity accounting share one physical root. One boundary is not exact: cesium cycles per NTP fractional tick equal 2.140326372..., a non-integer ratio recorded here as a raw measurement with no further identification made.

## 3. Receipt Layouts and Frame Alignment

Four layouts were measured. All contain only kernel-derived quantities plus the NTP coordinate and, in L16 and L20, an era byte.

| Layout | Fields | Bytes | Depth-4 frames | QR fit |
|--------|--------|-------|----------------|--------|
| L16 | time 8, identity 3, seal 3, event 1, era 1 | 16 | 4, remainder 0 | V1-L and above |
| L17 | time 8, identity 3, seal 3, parity 3 | 17 | 4, remainder 1 | V1-L exactly and above |
| L18 | L17 + event 1 | 18 | 4, remainder 2 | V2-L/M/Q |
| L20 | L18 + era 1 + pad 1 | 20 | 5, remainder 0 | V2-L/M/Q |

The era byte resolves the 2036 sec32 wraparound inside the payload instead of by external convention: 256 eras of 136.1 years give 34,841 years of unambiguous time for one byte. Frame alignment is ledger hygiene for the genealogy layer: frame records and divergence localization operate on 4-byte frames, so a receipt stream whose records are frame-aligned (L16, L20) keeps the archive's frame grid stationary across record boundaries, while L17 and L18 drift the grid by one and two bytes per record respectively. Verification recomputes every field from (identity bytes, payload); in the measured tamper case (one payload bit flipped), identity passes while seal, parity, and event all fail, giving three independent detection layers.

## 4. QR Version Selection

Byte-mode data capacities: Version 1 holds 17/14/11/7 data bytes at error-correction levels L/M/Q/H; Version 2 holds 32/26/20/14. The selection rule is minimal enclosure, the smallest version whose capacity covers the receipt. L16 and L17 fit V1-L; L18 and L20 require V2. The version number then acts as a layout discriminator at scan time with no version field spent inside the payload, and the V2-L frame's 32 data codewords match the register-atom width.

## 5. The Z₂ Holonomy Cycle

The wavefunction analysis establishes that the canonical four-byte word F = W2 ∘ W2′ closes one CGM frame and lands the carrier on the conjugate Z₂ sheet. The complete round trip is the operator product F squared. That is two F-words and eight ledger bytes, returning the carrier to rest. Theorem T6 of that analysis states that F is the K4 operator product of two depth-four half-words. The eight-byte object is the involution F² of that product. This was remeasured directly. For all 64 micro-references, F(F(rest)) = rest (64/64). The micro-0 F-word is bytes AA AB 2A 2B, and the carrier traverses 0xAAA555 to 0x555AAA to 0xAAA555. The Z₂ oscillation has period 2 in the word-count variable. The receipt's time field is 8 bytes, exactly one complete Z₂ holonomy cycle wide. The 16-byte aligned layouts are exactly two cycle widths, four complete depth-four frames. The 17- and 18-byte layouts are two cycle widths plus one or two bytes that open the next frame. This is a width identity, stated without a dynamical claim. The moment's time coordinate occupies the same ledger width as the full rest-to-swapped-to-rest round trip of the carrier.

## 6. The Daily Geometry: MU as Occupied Time

The whitepaper defines MU as occupied coordination capacity and fixes the base rate at 1 MU = 1 minute, 60 MU = 1 hour. The accounting unit is therefore denominated in the day itself, and the kernel carrier is a 24-bit state: 24 bits against 24 hours, 12 bits per face against 12 hours, 6 dipole pairs per face against 6 two-hour blocks. Measured: 240 x 6 = 1440 (UHI minutes times six is the full day in minutes), 4 x 6 = 24 (UHI hours times six is the full day), 2 x 6 = 12 (UHI pairs times six is the full pair count). The unconditional baseline occupies exactly one sixth of the daily carrier, 2 of 12 dipole pairs, and this sixth appears at all three grains simultaneously. The tier schedule resolves into occupation fractions of one face and one grain refinement.

| Tier | Role (whitepaper §12) | MU/day | Hours/day | Dipole pairs | Occupation |
|------|----------------------|--------|-----------|--------------|------------|
| 1 | Intelligence Cooperation | 240 | 4 | 2 | 1/3 of one face, 1/6 of the day |
| 2 | Inference Interaction | 480 | 8 | 4 | 2/3 of one face, 1/3 of the day |
| 3 | Information Curation | 720 | 12 | 6 | one full face, 1/2 of the day |
| 4 | Governance Management | 14,400 | 4 at second grain | 2 at 60x resolution | same extent, finer grain |

Tier 3 occupies one complete face of the carrier, the full half-day. Tier 4 does not extend the day, which would be impossible beyond 24 hours; its multiplier 60 equals MU_PER_HOUR exactly (measured), so Tier 4 is the same 4-hour extent resolved in seconds: 14,400 seconds/day, and 14,400 x 365 = 5,256,000 (measured). The tier ladder is thus a geometry of the day, not a schedule of work: baseline existence occupies one sixth of the daily carrier, full recognition occupies one face, and governance occupation refines the grain sixtyfold within the baseline extent. This reading is consistent with the whitepaper's definition of MU as occupation of capacity rather than a balance, and with its tier definitions as UHI multipliers rather than work schedules.

## 7. The Holographic Structure of the Commitments

The manifold obeys the holographic identity |H|^2 = |Omega| = 4096, and the general law |Space| = |Subspace|^2 with exactly 50% redundancy, where the specification identifies the redundancy with provenance: the dual reading related by the fold map P. Each 24-bit seal in the receipt therefore carries 12 bits of manifold coordinate and 12 bits of dual face, the A and B components. The redundant half is not overhead; it is the provenance channel that makes the coordinate self-locating, and it is why an Omega state is compressible to 8 bits (6 bits of horizon coordinate plus 2 bits of K4 gauge, a 33.3% structural compression noted in the QuBEC theory) yet is transmitted in the receipt in its 24-bit form: the receipt spends bytes on the dual reading because the dual reading is the verifiable content. The same holographic law operates at the scale of history: a 24-bit seal anchors an unbounded byte history to a manifold coordinate, because the kernel's ledger degeneracy (many histories reach one state) is precisely the mechanism by which a fixed-size coordinate can stand for a history of any length. Uniqueness of the history is supplied by the name layer and by frame records; verifiable position is supplied by the seal; the division of labor is structural, and no byte in the commitment fields is idle.

## 8. The FNV Name Layer

FNV-1a was measured as the candidate name layer, on the strength of its algebraic shape: each step is (hash XOR byte) then multiply, the same (XOR then transform) shape as the kernel's mutation-then-gyration step, and its primes are built as 256^t + 2^8 + b, a byte-native form (measured: 0x100000001B3 = 2^40 + 2^8 + 0xB3, popcount(0xB3) = 5 in the required {4, 5} set). The name value is derived: because it is a deterministic function of the payload, it costs nothing to store in the archive, since any verifier or archive regenerates it from the same content that drives the seal. What the name layer supplies is the archive's append gate, the recognition that a re-presented receipt is a replay and a new receipt is admissible, and under the trajectory model that gate is the identity's own depth log: a re-presented depth on an already-occupied trajectory coordinate is a replay, and the gate is a 4096-bit occupancy bitmap per trajectory epoch (512 bytes, one bit per frame coordinate), exact and constant-size, local to each archive. Measured dispersion: 10,000 sequential payloads give 10,000 distinct 64-bit names with zero collisions. The name value is also not inert: its 8 bytes are one Z2-cycle-width word, and routing them through the kernel yields a second, independently projected on-Omega coordinate (measured: fiber24 = 0x595966, on Omega), so the pair (seal24, fiber24) forms a 6-byte joint content coordinate that is near-unique at ledger scale (9,954 distinct pairs in 10,000 payloads). Two measured weaknesses bound its use. The documented XOR-correlation identity holds exactly for FNV-1 (last-byte delta exactly 0x3, multiply-then-XOR is XOR-linear in the trailing byte) and fails for FNV-1a (delta 0x1000000025D); the multiply in FNV-1a is precisely what breaks the linearity, so the correlation weakness belongs to FNV-1 and the FNV-1a avalanche is the correction. And FNV m12 bucketing of Omega is non-uniform under structured naming (max/ideal 8.192, empty buckets at n = 100,000), so manifold addressing uses time-derived m12 only; the name layer names, and does not address. Its non-cryptographic character fits the medium: dispersion without artificial scarcity, with integrity carried by the kernel seal, the parity codewords, and the event byte.

## 9. Capacity Convergence

The ledger's storage discipline follows from the kernel's own time model: temporal progression in the kernel is ledger depth, and the byte log is the archive's native record. An identity's receipt stream is a single deterministic trajectory through Omega beginning at one genesis anchor. A receipt is therefore fixed by three coordinates: the anchor (the trajectory origin, stored once per identity as a 3-byte routed state), the depth (the frame index along the trajectory, stored as a one-byte delta per receipt), and the phase (the position within the depth-4 frame, read from the low two bits of the depth). The seal, parity, event, and time fields of the transport form are all recomputed from these coordinates by replay, at multiply-free XOR cost, and the holographic identity |H|^2 = |Omega| reduces any checkpoint snapshot to one byte. The recurring capacity question then resolves into a single accounting table, all entries measured. The storage rows use the coordinate ledger: the 17-byte form is the transport encoding, not the archive encoding.

| Layer | Capacity | Human-scale need | Headroom |
|-------|----------|------------------|----------|
| Time addresses, m12 + 1 discriminator byte | 3.306789 x 10^13 per year | 2.9565 x 10^12 receipts per year | 11.18x |
| MU capacity envelope (CSM) | 7.944165 x 10^26 MU, fixed | 7.0956 x 10^14 MU per year (8.1 x 10^9 at UHI) | 1.12 x 10^12x |
| Receipt storage, coordinate ledger | 1 byte per receipt | 2.96 TB per year, all humanity | one consumer drive |
| Identity anchors | 3 bytes per identity, stored once | 24.3 GB total | negligible |
| Append gate | 512 bytes per trajectory epoch | constant per identity | negligible |

The daily event density is 22.89 receipts per m12 bucket, which the discriminator byte absorbs at a load factor of 0.089. The MU row above assumes universal Tier 1; the tier-resolved coverage of the envelope by universal occupation at each tier was also measured.

| Universal occupation | Annual demand (MU) | CSM coverage (years) |
|---------------------|--------------------|----------------------|
| All 8.1 x 10^9 at Tier 1 | 7.095600 x 10^14 | 1.119590 x 10^12 |
| All 8.1 x 10^9 at Tier 2 | 1.419120 x 10^15 | 5.597952 x 10^11 |
| All 8.1 x 10^9 at Tier 3 | 2.128680 x 10^15 | 3.731968 x 10^11 |
| All 8.1 x 10^9 at Tier 4 | 4.257360 x 10^16 | 1.865984 x 10^10 |

The answer to whether the medium fits the financial needs of all humanity for a year is therefore affirmative and quantitative at every layer simultaneously, and it survives the strongest possible demand assumption: even with every person on Earth occupying at Tier 4, the envelope covers 18.66 billion years of the full human economy. The one-year time grid addresses humanity's annual unconditional income eleven times over, the capacity envelope exceeds universal Tier 1 demand by twelve orders of magnitude, and the entire species' annual ledger is about 3 TB of depth deltas plus 24.3 GB of anchors, a single consumer drive. Scarcity appears at no layer of the stack: not in addresses, not in capacity, not in transport (commodity QR), not in storage, not in names (derived values).

## 10. Inference Hosts and the Production of Receipts

The kernel is a byte-driven finite transducer, so anything that drives it with bytes advances ledger depth. The runtime specification places transformers among its four bridge domains, and the repository carries a native backend hook into llama.cpp, with external tensors tiled into the kernel's native 64-wide blocks under structured P_Q plus residual D_Q contraction and with softmax and cosine similarity replaced by integer algebra in a live 1B-parameter model. These are documented properties of the runtime rather than measurements of the two scripts recorded here, and they are cited because they change what a receipt costs to produce.

Under that arrangement model execution and trajectory advance are one execution. The three coordinates of the coordinate ledger, the anchor, the depth and the phase, are properties of a trajectory the workload is already tracing, and the transport form of the receipt layouts is a readout of a position rather than a separate accounting event. The archive consequence follows the same way: the append gate is the host's own depth log, and the 512-byte occupancy bitmap per trajectory epoch is maintained by the same execution that performs the inference. Marginal receipt cost reduces to reading three coordinates and emitting the transport bytes.

This is also where the whitepaper's operational gap closes. Whitepaper §3 makes the verified AI inference event the primary alignment artifact, and §11 asks for a production genealogy, the governed record of a machine-mediated act, as the bridge between inference and economic attribution. On a kernel-hosted inference path the inference act is the byte word, the production genealogy is the frame record of that word, and the receipt is the same object in transport form: one execution on three charts, consistent with the multi-chart reading of the receipt. What the kernel supplies is the replayable machine-production record. Oversight class, consent scope and acceptance conditions are application-layer governance under The Human Mark and are not derived from the trajectory.

Two boundaries are stated to prevent overreach. First, receipt validity does not depend on model identity. Verification is deterministic recomputation from the receipt coordinates under the fixed transition law, so hosts holding different weights, or no model at all, verify identically. What is agreed prior to any claim is the arithmetic of the medium, not consent to a particular occupation; consent remains a governance act with its own attestation. Second, where a model is used for classification, routing or assistance, reproducibility of that classification does require a stable model identifier, which is governance metadata attached to the record and not part of the sealed coordinate.

The deployment consequence is availability rather than a new width identity. Inference hosts are already installed at planetary scale, in data centers, on consumer devices and inside enterprise systems, for utility unrelated to settlement. Attaching receipt creation, scanning, verification, routing and local archive maintenance to that installed base removes the dedicated node buy-in that a replicated ledger would otherwise impose, and it matches the offline receipt mode that whitepaper §12 permits, since a host can accept and route locally and synchronize later. Whitepaper §3 observes that the growth of AI compute is one of the first ways the economy becomes physically active at scale; co-execution is the mechanism by which that holds, because the compute that grows is the compute that traces the trajectories.

## 11. Structural Reading

The substrate of the medium is inherited, not built. Four pre-existing layers interlock by width identities: the atomic time standard and its NTP fixed-point grid (running since 1900, 1.72 x 10^19 elapsed coordinates), the kernel's fixed finite geometry (Omega, the horizons, the mask code), commodity QR transport with Reed-Solomon correction, and a derived dispersion hash for names. A fifth layer is availability of execution rather than a further width identity: the installed base of inference hosts already runs the kernel in the model execution path, so trajectories exist as a byproduct of unrelated workloads and receipts read positions on them. The architecture's contribution is the alignment: m12 makes time manifold-addressable, the routed seal makes content manifold-addressable, the parity field places content on the code chart, the event byte places it on the chirality and gauge charts, the F-squared cycle width matches the time field width, and the accounting units are the calendar of the carrier. A moment receipt is thus a point where the time grid, the manifold, the code, and the transport meet, transported in 16 to 20 bytes and archived as a single trajectory coordinate: an economy conducted in such receipts is an occupancy discipline over coordinates that already exist, with moments claimed, bound to identities and content, verified by recomputation, and remembered by trajectory depth logs, with double-claiming of a coordinate detectable locally within any archive and no central arbiter required for the arithmetic of the medium. The whitepaper's civic interface (§12) specifies that local communities may accept and route MU through offline receipts and synchronize later, with continuous synchronization not required; the QR layouts are the operational form of that offline receipt mode, and the genealogy layer of the analysis script is the replayable proof of inhabited capacity that whitepaper §8 assigns as the institutional body of the economy.

## 12. Open Items

- The era byte (L16, L20) is the proposed in-payload resolution of the 2036 sec32 wrap; without it, era inference remains an external convention.
- The intra-bucket discriminator byte requires an allocation policy (issuer-assigned, sequential, or content-derived) at event densities above one per bucket.
- FNV m12 addressing is non-uniform under structured naming; the name layer names and does not address.
- Binding an identity anchor to a person is an application-layer concern outside the kernel and outside this analysis.
- If an inference host is used for classification or routing, record a stable model identifier, for example a weight hash or signed version tag, as governance metadata, so that classification can be audited and reproduced under the same model build. Receipt validity itself is independent of model identity.
- Which segments of a model execution constitute a governed machine-production event, and how supervisory and acceptance conditions attach to them, is an application-layer policy outside the kernel and outside this analysis.
