[pytest]
markers =
    slow: marks tests that run with extended runtime
filterwarnings =
    ignore:.*torch\.jit\.script.*:DeprecationWarning
# Genomics lab tests live next to the package (not only under tests/).
testpaths =
    tests
    src/tools/autoencoder/programs/genomics/tests
